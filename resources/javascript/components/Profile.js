import React from 'react';
import Topbar from './Topbar';

const Profile = () => (
  <div className="app-container">
    <Topbar />
    <div>
      profile
    </div>
  </div>
);

export default Profile;
